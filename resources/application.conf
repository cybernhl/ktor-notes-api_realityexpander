ktor {
    deployment {
        port = 8001
        // sslPort = 8002 // <--- 正確的單行註解

        port = ${?PORT}
        dataSource = "SQLITE"
    }
    application {
        modules = [com.realityexpander.ApplicationKt.module]
    }

   #  security {
  # ssl {
    #         keyStore = "root/home/keys/ktornoteapp.jks"
    #         keyAlias = "ktor_note_app"
    #         keyStorePassword = "Zapper1000%^&"
    #         privateKeyPassword = "Zapper1000%^&"
    #     }
    # }
    # highlight-end
}
